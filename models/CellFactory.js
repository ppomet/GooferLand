const debugLog = require('debug')('debug-Cell-Factory');
const infoLog = require('debug')('info-Cell-Factory');
const errorLog = require('debug')('error-Cell-Factory');
const conf = require('config');
const Cell = require('./Cell');


const CellFactory = (options) => {
  return {};
    this.x = x;
    this.y = y;
    this.food = null;
    this.gooferName = null;

    this.GenerateCell = (options) => {

    };

    this.GenerateEmptyCell = () => {

    };
  }
}

module.exports = CellFactory;
